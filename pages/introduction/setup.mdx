## How to install

At the route of your React application, in your terminal run:

```zsh copy
npm install goopubtag
```

After installation, you can start using `goopubtag` in your application.

```tsx showLineNumbers
import { GPTProvider, GPTSlot } from 'goopubtag';

const Component = () => {
  return (
    <div>
      <GPTProvider networkId={123456}>
        {/** Your code.. */}
        <GPTSlot slotId="abc">
      </GPTProvider>
    </div>
  );
}
```

To learn more about our supported components and how to wire them up, have a look at:

- [GPTProvider](/components/gpt-provider)
- [GPTSlot](/components/gpt-slot)
- [useGPT](/components/use-gpt)

Or if you want to see how you can implement different use cases, check out our [examples](/examples/index).